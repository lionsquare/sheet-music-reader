#include <string>

class Reader {
    public:
        int import_file(std::string filename);
        bool process(void);
        void print_results(void);
    private:
        //string file_name;
        //Reader::open_file();
        //Reader::import_image();
        //Reader::close_file();
};
